from django.urls import path
from . import views

app_name = '_fundraiser'
urlpatterns = [
    # path('', views.account_index, name='account-index'),
    # path('_a/', views.admin_index, name='admin-index'),
    # path('sch/', views.search_user, name='search-user'),
    # path('tok/<int:id>', views.token, name='recharge-token'),
    # path('partake/', views.partake, name='partake'),
    # path('_s/', views.select, name='select'),
    # path('clm/', views.claimFunds, name='claimfunds'),
]
